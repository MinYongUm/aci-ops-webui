# ============================================================
# requirements-dev.txt
# 목적: 개발/테스트 의존성 (CI 파이프라인 및 로컬 개발 환경 전용)
# 적용 환경: GitHub Actions CI 러너, 로컬 개발 환경
# 설치 방법: pip install -r requirements-dev.txt
#            (requirements.txt를 자동으로 함께 설치)
#
# [주의]
# - Docker 이미지 빌드 시 사용하지 않음
# - "-r requirements.txt" 로 런타임 패키지를 먼저 포함시킴
#   → 개발 환경 세팅은 이 파일 하나로 완결
# ============================================================

# 런타임 의존성 전체 포함 (requirements.txt 참조)
-r requirements.txt

# Python 코드 스타일 검사 — PEP 8 준수 여부 확인 (CI에서 자동 실행)
flake8==7.2.0

# Python 코드 포매터 — 코드 스타일 자동 정렬 (CI에서 자동 실행)
black==25.1.0

# 테스트 프레임워크 — API 엔드포인트 단위 테스트 실행 (CI에서 자동 실행)
pytest==8.3.5